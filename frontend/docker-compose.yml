services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: orbit-frontend
    ports:
      - "3000:3000"
    restart: always
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
